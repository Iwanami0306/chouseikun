class RemoveAddColumnEventId < ActiveRecord::Migration[5.1]
  def change
    remove_column :join, :event_id, :string
    add_column :join, :event_id, :integer
    add_foreign_key :join, :event
  end
end
